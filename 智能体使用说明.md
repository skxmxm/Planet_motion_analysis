# 行星历表智能体使用说明

## 概述

行星历表智能体（Planetary Ephemeris Agent）是一个自动化的天文计算助手，能够：
1. **解析自然语言查询**：理解用户的日期、行星和精度要求
2. **自动选择计算模型**：在 VSOP87 和 DE430 之间智能选择
3. **估计计算误差**：给出误差上界和有效数字位数
4. **生成对比可视化**：自动绘制轨道差异对比图

---

## 快速开始

### 基本使用

```python
from planetary_ephemeris_agent import PlanetaryEphemerisAgent

# 创建智能体
agent = PlanetaryEphemerisAgent()

# 查询 2050 年火星位置，要求精度小于 10^-6
result = agent.query("查询 2050 年火星位置，要求精度小于 1e-6")

# 生成对比图
agent.generate_comparison_plot(result, "mars_2050_comparison.png")
```

---

## 查询语法

### 支持的查询格式

| 查询示例 | 解析结果 |
|---------|---------|
| "查询 2050 年火星位置，要求精度小于 10^-6" | 行星=火星, 年份=2050, 精度=1e-6 |
| "查询 2025 年地球位置" | 行星=地球, 年份=2025, 精度=1e-6（默认） |
| "查询 3000 年金星位置，精度 1e-8" | 行星=金星, 年份=3000, 精度=1e-8 |
| "查询 2100 年木星位置，要求精度小于 1e-10" | 行星=木星, 年份=2100, 精度=1e-10 |

### 支持的行星

- 地球、火星、金星、水星
- 木星、土星、天王星、海王星

---

## 智能体决策逻辑

### 模型选择流程

```
用户查询
    ↓
解析查询（年份、行星、精度）
    ↓
判断时间范围
    ├── < 200 年 → 两种模型都可用
    │   └── 根据精度要求选择
    │       ├── 高精度 (1e-10+) → DE430
    │       └── 普通精度 → VSOP87（更快）
    │
    └── > 200 年 → 只能使用 VSOP87
        └── 给出精度警告
```

### 选择依据

| 因素 | VSOP87 | DE430 |
|------|--------|-------|
| **时间范围** | 4000 年 | ~200 年 |
| **计算速度** | 快 | 较慢 |
| **短期精度** | 1e-8 | 1e-14 |
| **长期精度** | 1e-4 | 不适用 |
| **数据大小** | ~200KB | ~100MB |

---

## 输出结果说明

### 1. 模型推荐

```
推荐模型: VSOP87
备选模型: DE430
估计误差: 9.00e-09
置信度: 高
推荐理由: 两种模型均满足精度要求，推荐 VSOP87（计算更快）
```

### 2. 误差估计

```
黄经误差: 2.16e-06 rad = 0.4452 角秒
黄纬误差: 2.16e-07 rad
距离误差: 1.50e-10 AU = 0.02 km
```

### 3. 计算结果

```
儒略日: JD = 2469807.50
黄经 L: 100.7566861190°
黄纬 B: 0.0000000000°
距离 R: 0.9834522283 AU
计算时间: 0.0065 s
```

### 4. 精度验证

```
要求精度: 1.00e-06
估计误差: 1.50e-10
是否满足: ✓ 满足
```

---

## 可视化功能

### 生成的对比图包含

1. **黄经对比图**：VSOP87 vs DE430 的黄经曲线
2. **距离对比图**：VSOP87 vs DE430 的距离曲线
3. **黄经差异图**：两者黄经差异（角秒）
4. **距离差异图**：两者距离差异（公里）

### 示例输出

```python
# 生成并保存对比图
agent.generate_comparison_plot(result, save_path="comparison.png")
```

---

## 完整示例代码

```python
from planetary_ephemeris_agent import PlanetaryEphemerisAgent

# 创建智能体
agent = PlanetaryEphemerisAgent()

# 示例 1：短期高精度查询
print("=" * 70)
result1 = agent.query("查询 2025 年地球位置，要求精度小于 1e-10")
agent.generate_comparison_plot(result1, "earth_2025.png")

# 示例 2：长期查询
print("\n" + "=" * 70)
result2 = agent.query("查询 3000 年火星位置")
agent.generate_comparison_plot(result2, "mars_3000.png")

# 示例 3：不同精度要求对比
print("\n" + "=" * 70)
result3 = agent.query("查询 2050 年木星位置，要求精度小于 1e-4")
agent.generate_comparison_plot(result3, "jupiter_2050.png")
```

---

## 注意事项

### 精度限制

1. **VSOP87**：
   - < 100 年：精度 ~1e-8
   - 100-1000 年：精度 ~1e-6
   - > 1000 年：精度 < 1e-4

2. **DE430**：
   - 时间范围：约 1550-2650 AD
   - 精度：~1e-14

### 误差来源

| 误差类型 | VSOP87 | DE430 |
|---------|--------|-------|
| 模型误差 | 较大（简化假设） | 较小（N体积分） |
| 截断误差 | 显著（级数截断） | 较小（切比雪夫） |
| 舍入误差 | ~1e-16 | ~1e-16 |

### 使用建议

1. **短期（< 100 年）**：优先使用 VSOP87，计算快且精度足够
2. **中期（100-1000 年）**：根据精度要求选择，高精度用 DE430
3. **长期（> 1000 年）**：只能使用 VSOP87，注意精度限制
4. **航天任务**：必须使用 DE430 或更新的 DE 系列

---

## 扩展开发

### 添加新的行星模型

```python
class CustomPlanetModel:
    def compute_position(self, jd: float) -> tuple:
        # 实现自定义行星位置计算
        L, B, R = ...
        return L, B, R
```

### 修改模型选择策略

```python
class CustomModelSelector(ModelSelector):
    @classmethod
    def select_model(cls, request: QueryRequest) -> ModelRecommendation:
        # 自定义选择逻辑
        ...
```

---

## 文件结构

```
Planet_motion_analysis/
├── planetary_ephemeris_agent.py   # 智能体主程序
├── 任务二/
│   ├── vsop87_earth.py           # VSOP87 模块
│   └── de_chebyshev.py           # DE 插值模块
├── 任务三/
│   └── error_analysis.py         # 误差分析模块
└── 智能体使用说明.md             # 本文件
```

---

## 依赖安装

```bash
pip install numpy matplotlib
```

---

## 常见问题

### Q: 为什么 3000 年的查询只能使用 VSOP87？
A: DE430 的时间范围约为 1550-2650 AD，超出此范围没有数据。

### Q: 误差估计准确吗？
A: 误差估计基于任务3的实验结果，是保守估计（上界），实际误差可能更小。

### Q: 可以计算其他行星吗？
A: 当前版本支持八大行星，但需要相应的 VSOP87 系数文件。

### Q: 如何获取更精确的 DE430 数据？
A: 可从 NASA JPL 官网下载（https://ssd.jpl.nasa.gov/planets/eph_export.html）
